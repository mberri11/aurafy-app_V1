// Insights feed — 5th bottom-nav tab. Implementation lives in src/screens so the
// route file is a thin re-export (mirrors the project's screen/route split for
// the Insights feature). Tab registration: app/(tabs)/_layout.tsx.
export { default } from '@/src/screens/insights/InsightsScreen';
